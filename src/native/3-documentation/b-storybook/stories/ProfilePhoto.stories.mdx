import { Meta, Story } from "@storybook/addon-docs";

import josh from "../../../../images/josh.jpg";
import { ProfilePhoto } from "../components";

<Meta title="ProfilePhoto" component={ProfilePhoto} />

<!-- 👇 We create a “template” of how args map to rendering -->

export const Template = (args) => <ProfilePhoto {...args} />;

<!-- 👇 Each story then reuses that template -->

This component is meant to be used in place of any Twitter user's profile, such as alongside their tweets.
Please use it sparingly -- only when we need a shorthand visual representation of them.
Blah blah blah.

<Story
	args={{
		bordered: false,
		size: "large",
		src: josh,
	}}
	name="Story"
>
	{Template.bind({})}
</Story>
